import { Container } from '@chakra-ui/react'
import { FC } from 'react'

const CompetitionDetail: FC<{ id: string }> = ({ id }) => {
  const {}
  return <Container maxW='container.xl'></Container>
}

export default CompetitionDetail
